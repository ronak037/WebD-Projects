<%- include('partials/header'); -%>

<h1>Home</h1>
<p><%= startingContent %></p>

<!-- other way to loop and print all the elements -->
<% posts.forEach(function(post){ %>
    <h1><%= post.title %></h1>
    <p><%= post.content.substring(0,100) %>
        <% if(post.content.length>100){ %>
            <a> ...</a>
            <!-- if anyone clicks read more then it redirects to /posts/post-title defined in app.js..... continue1 -->
            <a href="/posts/<%= post.title %> ">Read More</a>
        <% } %>
    </p>
<% }); %>

<%- include('partials/footer'); -%>
</posts.length;i++){>
