<%- include("header") -%>

<div class="box" id="heading"><h1><%= listTitle %></h1></div>

<!-- <% if(listTitle==="Saturday" || listTitle==="Sunday"){ %>
    <h1 style="color: purple"><%= listTitle %> ToDo List</h1>
    <% } else { %>
        <h1 style="color: blue"><%= listTitle %> ToDo List</h1>
        <% } %> -->

<div class="box">
  <% newListItems.forEach(function(item){ %>
  <form action="/delete" method="post">
    <div class="item">
      <input
        type="checkbox"
        name="checkbox"
        value="<%= item._id %>"
        onchange="this.form.submit()"
      />
      <p><%= item.name %></p>

      <!-- new hidden item to get the correct name of the page to delete the items properly -->
      <input type="hidden" name="listName" value="<%= listTitle %>"></input>
    </div>
  </form>
  <% }); %>

  <form class="item" action="/" method="POST">
    <input
      type="text"
      name="newItem"
      placeholder="New Item"
      autocomplete="off"
    />
    <button type="submit" name="list" value="<%= listTitle %>">+</button>
  </form>
</div>

<%- include("footer") -%>
